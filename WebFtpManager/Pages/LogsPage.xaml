<Page x:Class="WebFtpManager.Pages.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#F5F7FB"
      Loaded="Page_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Background="#FFFFFF" Padding="24,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <RadioButton x:Name="RbAccess" Content="访问日志" Margin="0,0,12,0" IsChecked="True" Checked="Type_Changed"/>
                    <RadioButton x:Name="RbError"  Content="错误日志" Margin="0,0,12,0" Checked="Type_Changed"/>
                    <RadioButton x:Name="RbStats"  Content="统计日志" Margin="0,0,12,0" Checked="Type_Changed"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="行数:" VerticalAlignment="Center" Margin="16,0,6,0" Foreground="#6B7280"/>
                <ComboBox Grid.Column="2" x:Name="LinesCombo" Width="100" VerticalAlignment="Center" SelectedIndex="1">
                    <ComboBoxItem Content="200"/>
                    <ComboBoxItem Content="500"/>
                    <ComboBoxItem Content="1000"/>
                    <ComboBoxItem Content="3000"/>
                </ComboBox>
                <CheckBox Grid.Column="4" x:Name="AutoRefreshCb" Content="自动刷新 3s" VerticalAlignment="Center" Margin="0,0,12,0" IsChecked="True"/>
                <Button Grid.Column="5" x:Name="RefreshBtn" Content="刷新" Style="{StaticResource PrimaryButton}" Padding="12,6" Click="Refresh_Click"/>
            </Grid>
        </Border>

        <!-- 日志内容 -->
        <Border Grid.Row="1" Background="#0F172A" Margin="24,0,24,24" CornerRadius="8" Padding="0">
            <TextBox x:Name="LogBox" Background="Transparent" BorderThickness="0" Foreground="#E2E8F0"
                     FontFamily="Consolas" FontSize="12" IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap" Padding="12" Text="加载中..."/>
        </Border>
    </Grid>
</Page>
